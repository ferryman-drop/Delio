# ✅ TASK-011: Інтеграція Claude 3.5 Sonnet

## 🎯 Мета
Інтегрувати **Claude 3.5 Sonnet** як "Мудрого Суддю" (Wise Judge) для забезпечення високоякісного нагляду та покращення коду, діючи як альтернатива або доповнення до DeepSeek Critic.

## ⚙️ Деталі Реалізації

### 1. Залежності
- Встановлено бібліотеку `anthropic`.

### 2. Конфігурація (`config.py`)
- Додано `ANTHROPIC_KEY` з змінних середовища.
- Визначено `MODEL_JUDGE = "claude-3-5-sonnet-20241022"` (Остання стабільна версія Sonnet).

### 3. LLM Сервіс (`core/llm_service.py`)
- Реалізовано функцію `call_judge`.
- **Роль**: "Wise Judge" — фокусується на точності, корисності та дотриманні персони.
- **Протокол**: Приймає "Запит Користувача" + "Відповідь Актора" -> Повертає "Покращену Відповідь".
- **Обробка Помилок**: М'який відкат (Fallback) до оригінальної відповіді, якщо API недоступний.

## 🧪 Верифікація
- **Скрипт**: `scripts/test_claude.py`.
- **Статус**: Код інтеграції коректний, але тести показали помилку `404 Not Found` для ID моделі `claude-3-5-sonnet-20241022`. Це зазвичай вказує на те, що API ключ ще не має доступу до цієї конкретної моделі, або ID відрізняється.
- **Дія**: Код готовий. Бот продовжує стабільно працювати на Gemini/DeepSeek. Claude автоматично запрацює, коли проблему з ключем/ID буде вирішено.

## 🔜 Наступні Кроки
- Уточнити доступний Model ID для наданого ключа.
- Увімкнути маршрутизацію `preferred_model='claude'` у FSM.
