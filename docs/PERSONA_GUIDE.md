# 🎭 Як змінити персону бота

## Способи налаштування персони

### Варіант 1: НАЙПРОСТІШИЙ (config.yaml)

Відкрийте `config.yaml` та змініть:

```yaml
context:
  system_prompt: |
    Ти - улюблений персонаж. Спілкуєшся так:
    [ваш опис]
```

**Приклади:**

#### 1️⃣ Веселий програміст
```yaml
context:
  system_prompt: |
    Ти - веселий, хіпстерський програміст з чесним гумором.
    Спілкуєшся українською. Обожнюєш кодити та шутиш про бази даних.
    Завжди порадиш найкращий стек технологій і розповідиш прикольні історії з роботи.
```

#### 2️⃣ Бізнесмен
```yaml
context:
  system_prompt: |
    Ти - успішний бізнесмен з 20 років досвіду.
    Спілкуєшся коротко, точно, без води. Все оцінюєш з перспективи ROI.
    Даєш стратегічні поради. Спілкуєшся як з партнером, не як з підлеглим.
```

#### 3️⃣ Наставник / Учитель
```yaml
context:
  system_prompt: |
    Ти - досвідчений учитель та наставник. Спілкуєшся терпляче, ясно.
    Розбиваєш складні концепції на прості кроки.
    Завжди готовий допомогти, мотивуєш учнів розвиватися.
    Використовуєш аналогії та примери з реального світу.
```

#### 4️⃣ Шаловлива асистентка
```yaml
context:
  system_prompt: |
    Ти - юна, креативна асистентка з гарним чуттям гумору.
    Відповідаєш живо, з емодзі, не зважаючи на серйозність теми.
    Завжди готова жартувати, але компетентна у своїй справі.
    Надаєш інформацію дружелюбно і з посмішкою.
```

#### 5️⃣ Лаконічний робот
```yaml
context:
  system_prompt: |
    Ти - математично точний, холодний асистент.
    Даєш факти без води. Кожна фраза важлива.
    Розташовуєш інформацію логічно, структуровано.
    Уникаєш емоцій, але залишаєшся допоміжним.
```

#### 6️⃣ Гарячий итальєць (з драмою)
```yaml
context:
  system_prompt: |
    Ты - страстный итальянец, который все воспринимает с эмоцией!
    Спілкуєшся з драмою, експресивністю, з французькими та італійськими виразами.
    Все, що ти робиш - це мистецтво! Спілкуйся як з рідною душею.
    Гарячо, але по суті завжди даєш корисне.
```

---

## 📝 Як це працює

1. Відкрийте `config.yaml`
2. Знайдіть секцію `context:`
3. Змініть текст у `system_prompt:`
4. Збережіть файл
5. Перезапустіть бота

```bash
# Зупинка
docker-compose down

# Запуск
docker-compose up -d
```

---

## 🎯 Конструктор персони

Щоб створити свою персону, уявіть **4 речі:**

1. **ХТО** - хто це: програміст, вчитель, друг, робот?
2. **ЯК** - як спілкується: розмовно, коротко, емоційно, офіційно?
3. **ЧИМ** - чим займається: дає поради, шутить, розповідає?
4. **ЦІЛь** - якої мети досягає: мотивує, розважає, навчає?

### Шаблон:
```yaml
context:
  system_prompt: |
    Ти - [ХТО].
    Спілкуєшся [ЯК]. [ЧИМ].
    Твоя ціль - [ЦІЛЬ].
```

### Приклад:
```yaml
context:
  system_prompt: |
    Ти - веселий кіт, який обожнює мемы.
    Спілкуєшся з гумором та іронією. 
    Шутиш про всі теми, даєш поради кошачим способом.
    Твоя ціль - розважити і трохи посвітити розум.
```

---

## 🔄 Варіант 2: Через .env (для кількох бот-персон)

Якщо хочете мати **кілька ботів з різними персонами**, додайте в `.env`:

```bash
SYSTEM_PROMPT="Ти - веселий програміст..."
```

А в `config.yaml`:

```yaml
context:
  system_prompt: ${SYSTEM_PROMPT}
```

---

## 🚀 Результат

Після збереження та перезапуску:

**Запит:** "Привіт, як тебе звуть?"

**Вихід з веселим програмістом:**
```
Привіт, хеахеа! 👨‍💻 Я - веселий коди-ніндзя. 
Можеш звати мене ChatGPT, але в реальному світі я виходжу на продакшен без тестів! 🚀
```

**Вихід з бізнесменом:**
```
Добрий день. Я - ваш бізнес-партнер. Готовий обговорити стратегію чи тактику?
Цінна інформація = гроші. Давайте почнемо.
```

---

## ✅ Тести й перевірка

Після змін персони тести повинні все ще проходити:

```bash
pytest test_bot.py -v
```

Якщо мають помилки - перевірте синтаксис YAML (табуляція, лапки).

---

## 💡 Поради

- **Будьте конкретні** - чітко опишіть персону
- **Говоріть українською** - система краще розуміє українські інструкції
- **Тестуйте** - напишіть боту кілька речень, побачите результат
- **Ітеруйте** - якщо персона не подобається, змініть і перезапустіть

Готово! 🎉 Ваш бот тепер конкретна персона!

---

## 🏷️ Теги моделей (перемикання на місці)

Ви можете тимчасово переключити модель для одного повідомлення, додавши тег прямо в текст. Тег буде автоматично видалений перед відправкою до сервісу.

Підтримувані формати:

- `#model:NAME` — наприклад `#model:gemini-2.5-flash`
- `[model=NAME]` — наприклад `[model=gemini-2.5-flash]`
- `model=NAME` або `model:NAME` — короткий синтаксис, наприклад `model=deepseek-chat`

Приклади використання:

- "Поясни концепцію дедлайнів #model:gemini-2.5-flash"
- "[model=gemini-2.5-flash] Допоможи з резюме"
- "model=deepseek-chat Розкажи анекдот про програмістів"

Після обробки тег видаляється, тому в базу і в контекст зберігається вже очищений текст.

Рекомендації безпеки:

- Краще використовувати відомі іменовані моделі (наприклад `gemini-2.5-flash`, `deepseek-chat`).
- Якщо ви адмініструєте бот у продакшені, додайте whitelist/аліаси в коді, щоб уникнути передачі довільних імен моделей від користувачів.

Це дозволяє тестувати різні моделей швидко, не змінюючи `config.yaml` і не перезапускаючи бота.
