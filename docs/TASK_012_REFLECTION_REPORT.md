# 🪞 TASK-012: Активна Рефлексія (Active Reflection)

## 🎯 Мета
Впровадити механізм **Активної Рефлексії**, який дозволяє боту оцінювати власну ефективність після кожної взаємодії та зберігати "уроки" для уникнення помилок у майбутньому.

## ⚙️ Деталі Реалізації

### 1. База Даних (`data/bot_data.db`)
- Створено таблицю `lessons_learned` для зберігання критичних зауважень та інструкцій щодо виправлення поведінки.
- Кожен запис містить оцінку (`score`), критику (`critique`) та спосіб виправлення (`correction`).

### 2. Аналіз Продуктивності (`core/llm_service.py`)
- Додано функцію `evaluate_performance`, яка використовує **Gemini Flash** для швидкої та дешевої оцінки відповіді бота.
- Критерії оцінки: точність відповіді, відповідність тону та лаконічність.

### 3. Цикл Рефлексії (`states/reflect.py`)
- Після кожної відповіді бот викликає модуль оцінки.
- Якщо оцінка нижче **7/10**, зауваження автоматично зберігаються в базу знань як "засвоєний урок".

### 4. Навчання на помилках (`states/plan.py`)
- Перед генерацією нової відповіді система перевіряє останні 3 "уроки" для конкретного користувача.
- Ці уроки додаються в системний промпт як блок **⚠️ CRITICAL LEARNINGS FROM PAST MISTAKES**, що змушує модель адаптуватися та не повторювати помилок.

## 🧪 Верифікація
- Проведено успішне тестування за допомогою скрипта `scripts/test_reflection.py`.
- Модель Gemini Flash успішно ідентифікувала "слабку" відповідь (2+2 = "я філософ") та надала корекцію.
- Механізм ін'єкції уроків у промпт працює коректно.

## 🔜 Використання
Система тепер автоматично навчається на кожній невдалій взаємодії. Це дозволяєDelio ставати розумнішим та персоналізованішим для кожного користувача без ручного втручання.
